/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hide the floating dev-tools indicator (bottom-left "N" button).
  devIndicators: false,
};
export default nextConfig;
